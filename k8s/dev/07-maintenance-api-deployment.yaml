apiVersion: apps/v1
kind: Deployment
metadata:
  name: maintenance-api
  namespace: carlog-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: maintenance-api
  template:
    metadata:
      labels:
        app: maintenance-api
    spec:
      containers:
        - name: maintenance-api
          image: carlogmaintenanceapi:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: maintenance-api-config
            - secretRef:
                name: maintenance-api-secret
          resources:
            requests: { memory: "256Mi", cpu: "250m" }
            limits: { memory: "512Mi", cpu: "500m" }
