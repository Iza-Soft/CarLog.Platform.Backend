apiVersion: v1
kind: Service
metadata:
  name: maintenance-api
  namespace: carlog-dev
spec:
  selector:
    app: maintenance-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
