apiVersion: v1
kind: Service
metadata:
  name: vehicle-api
  namespace: carlog-dev
spec:
  selector:
    app: vehicle-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
